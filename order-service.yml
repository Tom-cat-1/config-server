server:
  port: 8082

product:
  service:
    url: http://localhost:8081

spring:
  application:
    name: order-service

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
